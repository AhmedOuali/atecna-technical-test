{
  "name": "root",
  "private": true,
  "scripts": {
    "start-front": "lerna run start --scope=@atecna/front",
    "start-server": "lerna run start:dev --scope=@atecna/server"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
